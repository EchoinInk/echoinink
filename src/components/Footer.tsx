export function Footer() {
  return (
    <footer className="absolute bottom-0 left-0 right-0 z-40 pointer-events-none">
      <div className="ei-container">
        <div className="flex items-end justify-between pb-8 md:pb-12">
          <span className="font-structural text-[10px] tracking-[0.25em] uppercase text-white/40">
            INK MEETS LIGHT.
          </span>
          <span className="font-structural text-[10px] tracking-[0.25em] uppercase text-white/40">
            EMOTIONS BECOME DESIGN.
          </span>
        </div>
      </div>
    </footer>
  );
}
